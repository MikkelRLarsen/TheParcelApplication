# ParcelService Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: parcelservice
  labels:
    app: parcelservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: parcelservice
  template:
    metadata:
      labels:
        app: parcelservice
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "parcelservice"
        dapr.io/app-port: "8001"
        dapr.io/enable-api-logging: "true"
    spec:
      containers:
        - name: parcelservice
          image: ghcr.io/mikkelrlarsen/parcelservice:9e9a5621dc6dee1f909ab10e20e983f5f31beceb
          ports:
            - containerPort: 8001
          imagePullPolicy: Always
          env:
            - name: ConnectionStrings__parceldb
              valueFrom:
                secretKeyRef:
                  name: parcel-db-secret
                  key: connectionString
